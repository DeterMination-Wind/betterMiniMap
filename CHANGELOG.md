# Changelog

## v1.0.0
- 🚀 首发 betterMiniMap：把小地图单位标记改为单位 `icon`，并支持朝向旋转显示。
- 🧠 新增同类型单位聚合：按小地图像素距离动态聚合，缩放变化时聚合阈值同步变化。
- 🎛️ 新增可调参数：单位聚合间隔（px）、图标缩放、透明度、单位/建筑筛选。
- 📦 完成发布流水线：自动构建 `.zip` / `.jar` / `-android.jar` 并自动发布 Release。
